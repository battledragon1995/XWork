{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Grants the main window only the permissions consumed by the current shell.",
  "windows": ["main"],
  "permissions": []
}
