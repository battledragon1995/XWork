# FE-003 Home — Phase 1 Stage 14 Implementation Plan

**Status:** FE-003 code and all automated Windows gates complete; ready for parent commit `Implement FE003`. Native / stage 14 / Phase 1 acceptance remains pending. No implementer commit or stage 15 work.

**Goal:** Replace the populated `/` placeholder with the real Phase 1 Home dashboard, using existing Projects and Sessions queries, while preserving Welcome, owner navigation, and the stage 13 maintenance lifecycle.

**Completion Criteria:**

- Home renders the specified recent projects and all current runtime sessions with truthful status/counts and working owner links; an empty project list still renders Welcome.
- Query, subscription, focus, unmount, import/reset and provider-lifetime regressions pass without accessing developer data.
- All final Windows frontend, Rust and Tauri gates below pass on the final source revision. Record counts, commands, exit codes and build output.
- Stage 14/Phase 1 acceptance remains pending until the native acceptance matrix is demonstrated. A code/automated handoff or feature commit does not imply native or performance acceptance.

**Architecture:** Home uses the existing IPC wrappers and generated DTOs, with one local Projects snapshot serving both presence and recent rows and a separate Sessions query lifetime. `HomeEntry` in app composition passes current maintenance/Quit state into the Home public entry; Home imports no other feature implementation. Existing providers, terminal owners and backend commands retain their lifetimes and responsibilities.

**Tech Stack:** Existing React/TypeScript, React Router memory routing, Tailwind/design tokens, copied UI primitives, Vitest/RTL and Tauri IPC. Existing Rust/Tauri gates remain required. No dependency additions, manifest changes or version selection are planned.

**Sources:**

- Rules: `AGENTS.md`, `PLANS.md`; template: `00-Docs/99-Template/04-Plan.md`.
- Roadmap: `00-Docs/98-Plan/00-Roadmap.md`, stage 14 and Phase 1 completion rules.
- Architecture/tooling: `00-Docs/00-Overview/01-TechStack.md`, `00-Docs/00-Overview/02-ProjectStructure.md`, current `package.json` and `vite.config.ts`.
- Requirements: `00-Docs/00-Overview/03-FunctionalRequirements.md`, §§5.1–5.2, 6, 7.5, 8, 18, 20 Phase 1.
- Primary specification: `00-Docs/02-Frontend/FE-003-home.md`; shell extension: `00-Docs/02-Frontend/FE-001-application-shell.md`, stage 14.
- Existing boundaries: `00-Docs/02-Frontend/FE-002-welcome.md`, `00-Docs/02-Frontend/FE-009-command-palette.md`, `00-Docs/02-Frontend/FE-014-settings-keyboard-shortcuts.md`, `00-Docs/02-Frontend/FE-015-settings-data.md`.
- Backend specifications: `00-Docs/03-Backend/BE-003-projects.md`, `00-Docs/03-Backend/BE-005-sessions-runtime.md`, `00-Docs/03-Backend/BE-012-backup-and-reset.md`.
- Wireframe: `00-Docs/01-Wireframe/03-Home.html#empty` and `#full`; apply the explicit Phase 1 reductions in FE-003.
- Historical evidence, read-only: `00-Docs/98-Plan/20260906-fe-015-settings-data.md`, final handoff and native-pending sections.

## Scope

**In Scope:**

- FE-003 dashboard, project-presence snapshot sharing, session snapshots, presentation projections, independent states, navigation, accessibility and FE-001 composition adapter.
- Regression coverage for Welcome, palette Home, maintenance epoch/live guards, import/reset and persistent provider identity.
- Documentation evidence in this active plan and necessary contract decisions in FE-003/FE-001; retain the existing uncommitted design documents for the later `Implement FE003` commit.

**Out of Scope:**

- New backend/query/endpoint/DTO/event/binding, Rust implementation, migrations, permissions, dependencies, arbitrary OS access or production mock IPC.
- Quick Note, Notes, Calendar, future-phase placeholders and shortcuts; Git/tool/duration/exit-code enrichment not available in the two summary DTOs.
- Home-owned project/session mutations, process creation, terminal output subscriptions, changes to existing palette executors or shortcut catalog.
- Independent review, subagents, staging or commits during this implementation turn. Historical plans remain untouched.

## Global Constraints

The following English constraints are copied from `PLANS.md` and apply to every task:

- “Creating a plan does not authorize implementation. Do not modify source code until the user requests implementation.”
- “React owns presentation and temporary UI state.”
- “Rust owns OS access, persistence, terminal processes, and business rules.”
- “Frontend features do not import implementation from other features.”
- “Generated bindings under `src/bindings/` are not edited manually.”
- “Tests must not read or write the developer's real app data, configuration, credentials, projects, or other user-owned state.”

Additional applicable requirements:

- Markdown uses UTF-8. This plan is English; feature specifications remain Vietnamese. Code, identifiers, UI and purpose comments are English. Every function, callback, helper and test needs a short purpose comment.
- Roadmap requires real runtime IPC now that BE-003/BE-005 exist; the older generic PLANS mock-IPC sentence does not authorize mocks in this production slice. Test doubles are confined to tests.
- Only Windows development checks; no automated desktop E2E; macOS deferred to release preparation.
- Use Cargo one job and Rust test threads one. Run gate processes sequentially, including frontend workers one; do not overlap builds or full suites.
- Preserve historical plans. Only this active plan receives execution evidence; unverified acceptance stays unchecked.

## Assumptions, Risks, and Blockers

**Assumptions:**

- Planning baseline HEAD is `92328af Implement FE015`, preceded by `452017f Implement BE012`. FE-003 and its FE-001 extension are already uncommitted design inputs with no open questions.
- The FE-003 inventory is authoritative. No other source file may be changed without first recording a concrete required scope extension in the specification.
- Session rows include all runtime statuses, ordered by attention priority, with running count based on `runningProcessCount > 0`. Recent projects are a sorted copy, at most five, not a reordered owner store.
- Public `HomeRouteProps`, `HomeBoundarySnapshot` and live `readBoundary()` are established in FE-003. New internal function names below do not create cross-feature APIs.

**Risks:**

- Provider context values are new objects after updates. Task 4 must preserve component/provider identity; derive dependencies from scalar busy/epoch fields, not a changing callback identity that causes query loops.
- A reset may keep `/` mounted, and stale promises may settle before effects. Tasks 1/2/4 assert render-time and live-boundary guards, not cleanup-only safety.
- Session events carry revisions but list responses do not. Use invalidation/coalescing, not a fabricated atomic cross-owner snapshot or invented query revision.
- Bridge tests currently mock `react-router`; they cannot alone prove route/provider lifetime. Task 4 adds real-router integration in `home-entry.test.tsx` or `app-router.test.tsx` while keeping the existing bridge collaborator tests isolated.
- Prior Rust suites have encountered terminal timing stalls and BE-011 timing failures. Sequential execution is required; failures remain evidence, and any targeted retry must be followed by the affected full gate. Do not weaken assertions or silently count a retry as the first pass.
- The existing frontend chunk advisory above 500 kB is historical, not a new performance result or authorization for bundling changes.

**Blockers:** None for Plan or implementation handoff. A verified disposable native Windows environment and appropriate measurement setup are still missing for native/performance acceptance. Do not substitute the developer's active profile or claim that acceptance complete.

## Dependency Order

1. Shared project snapshot and presence invariants → authoritative Home/Welcome branch.
2. Session query lifecycle and deterministic projections → truthful dashboard data.
3. Dashboard UI and owner links → observable Home behavior.
4. App boundary composition and real provider/router regression → safe maintenance integration.
5. Full gates, acceptance accounting and handoff → separately report code/automated readiness and remaining native work.

---

### Task 1: Share one project snapshot between Welcome presence and Home

**Outcome:** One Home-owned unfiltered Projects query supplies presence and recent-project data, with explicit refresh failure and maintenance generation semantics.

**Depends On:** None.

**Files:** Modify `src/features/home/use-project-presence.ts`, `src/features/home/home-route.tsx`; test `src/features/home/use-project-presence.test.ts`, `src/features/home/home-route.test.tsx`.

**Interfaces:** Consumes `listProjects()`, `onProjectsChanged(handler)`, `ProjectDto`, `ProjectChangedEventDto`, `IpcCallError`; produces the FE-003 snapshot/refresh/failure additions to `ProjectPresenceResult`, and the documented Home boundary types. Preserve `presence` and `refresh()` compatibility. Internal boundary argument may be optional for isolated existing hook tests; production must receive the app boundary in Task 4.

- [x] Add discoverable hook/route assertions first. Existing hook tests can access the proposed snapshot via a test-local structural cast for the initial red run, avoiding an unresolved import. Assert the supplied project array is exposed, presence and snapshot change together, and a refresh rejection with an existing snapshot keeps the Home branch and focus. The latter fails against the current hook that replaces presence with `failed`.
- [x] Run `pnpm test src/features/home/use-project-presence.test.ts src/features/home/home-route.test.tsx --maxWorkers=1 --no-file-parallelism`. Require nonzero discovered tests and capture the expected missing-snapshot/retained-branch assertion, not zero matches or module resolution failure.
- [x] Implement only the local snapshot extension. Register before initial query; on listener failure query anyway and expose retryable subscription degradation. Single-flight per source; invalidation during a request retires its result and queues one trailing read. Remove known deleted IDs immediately; never publish invalidated responses. Clean late subscriptions on unmount/StrictMode replay.
- [x] Cover first-load empty/present/errors, add/remove last project, failed refresh without false Welcome, focus/visible coalescing, registration rejection with manual recovery, delayed registration/unmount, dirty in-flight response, suspended/epoch change and failed post-reset read. Mock Projects wrapper, inject `IpcCallError`, deferred promises and an in-memory live boundary reader; no Tauri calls.
- [x] Rerun the exact focused command and `pnpm typecheck`. All selected tests pass with the old Welcome behavior preserved; no second query owner for recent rows.

### Task 2: Define deterministic presentation and the Sessions query lifetime

**Outcome:** Home obtains every runtime session via the existing global list and produces truthful stable projections without N+1 calls.

**Depends On:** Task 1.

**Files:** Create `src/features/home/home-presentation.ts`, `src/features/home/use-home-sessions.ts`; test `src/features/home/home-presentation.test.ts`, `src/features/home/use-home-sessions.test.ts`.

**Interfaces:** Consumes `listSessions()`, `onSessionsRuntimeChanged(handler)`, generated `SessionSummaryDto`/`SessionRuntimeEventDto` and full `ProjectDto[]`. Produces local projection functions for recent ordering, attention ordering, counts and timestamp/status labels, plus `useHomeSessions` returning the documented `HomeSessionsState`. The hook accepts enabled state, rendered/live boundary and project invalidation signal; these are internal UI inputs, not IPC DTOs.

- Planned checkpoint skipped (see execution deviations): Create compilable minimal local exports and their test modules before the red run. The initial recent projection may return the unsorted input, and the initial hook may expose an empty state without querying; record these as deliberate behavioral test baselines, never passing implementation.
- Planned checkpoint skipped (see execution deviations): Run `pnpm test src/features/home/home-presentation.test.ts src/features/home/use-home-sessions.test.ts --maxWorkers=1 --no-file-parallelism`. Expected red assertions: a sixth/older pinned project is not excluded/ordered correctly, and the enabled hook has not called `listSessions()` without arguments. Tests must load and execute.
- [x] Implement recent timestamp/tie-break/limit, stable six-status priority and separate running/attention counts exactly as FE-003. Project lookup uses the full array. Format absolute local dates with English labels and no invented duration/tool/branch data.
- [x] Implement subscription-first query, per-source flight/dirty guard, generation checks and listener recovery. Subscribe only while dashboard is enabled; resume with a fresh query after suspension. Project invalidation also invalidates Sessions; do not apply summary payloads as an aggregate query response. A removed-row result from an older flight cannot resurrect the row.
- [x] Assert all statuses, non-running sessions with live process counts, overlapping counters, zero/one/six projects, timestamp ties, unavailable project, missing label, event bursts with bounded concurrent calls, deletion/late response, failed subscription, failed query/retry, focus/visible, StrictMode/unmount, epoch changes and reconciliation of `projectNotFound` only once before explicit retry.
- [x] Rerun the exact focused command and `pnpm typecheck`; no N+1 `getSession`, Git query, process mutation or runtime polling. Assert calls at the mocked IPC boundary, not a fictional runtime command-registry API.

### Task 3: Render the Phase 1 dashboard and preserve Welcome navigation

**Outcome:** Home displays both blocks with usable keyboard navigation, truthful states and the specified empty/partial-error behavior.

**Depends On:** Tasks 1–2.

**Files:** Create `src/features/home/home-screen.tsx`; modify `src/features/home/home-route.tsx`; remove the now-unreferenced `src/features/home/home-placeholder.tsx`; test `src/features/home/home-screen.test.tsx`, `src/features/home/home-route.test.tsx`.

**Interfaces:** HomeScreen receives the full project snapshot/query state and boundary from HomeRoute and consumes the local Sessions hook/projections. Links target `/projects`, `/projects/:projectId`, `/sessions/:sessionId`; destination owners perform existing reads/opening. Public route entry remains `HomeRoute(props: HomeRouteProps): React.JSX.Element`.

- [x] The populated-route missing-heading assertion ran red in Task 1. HomeScreen direct tests were added with implementation rather than a separate stub/red; all are included in the final passing full suite.
- [x] Implement English UI, date header, counts, two responsive columns above 720 px content width and one below, skeleton/empty/error/stale/subscription states. Reuse existing primitives/tokens; no CSS dependency or new shared component abstraction.
- [x] Add one keyboard-accessible link per row, escaped route IDs and text labels/tooltips; do not block unavailable project overview. Use Open Projects for the empty-session CTA, not an implicit session mutation. Preserve keys/focus across sorting and move focus only when the focused content disappears.
- [x] Assert project/session destinations, no mutation on render/click in Home, unavailable/missing-label behavior, partial failure, retry locking, six statuses, full-list project-name joins and absent Quick Note/Notes/Calendar UI. Test Tab/Enter, icon labels, focus after reorder/deletion and no focus stealing from external controls/dialogs.
- [x] Use `vi.useFakeTimers()`/`vi.setSystemTime()` for header rollover with explicit timer advancement; use fixed numeric dates and current JS local date formatting rather than mutating process timezone. Restore timers/listeners after each test. Layout/a11y checks that jsdom cannot demonstrate remain native acceptance.
- [x] Rerun the same focused command plus `pnpm test src/features/home/welcome-screen.test.tsx --maxWorkers=1 --no-file-parallelism` and `pnpm typecheck`. Find remaining placeholder imports with `rg -n 'HomePlaceholder|home-placeholder' src`; expected no runtime references after removal. Historical documentation mentions are not deleted.

### Task 4: Compose maintenance and prove provider/router lifetime

**Outcome:** Home is safe across import/reset, uncertain reset, Quit and navigation without remounting application providers or terminal owners.

**Depends On:** Tasks 1–3.

**Files:** Create `src/app/home-entry.tsx`; modify `src/app/app-router.tsx`; test `src/app/home-entry.test.tsx`, `src/app/app-router.test.tsx`, `src/app/search-entry.test.tsx`, `src/app/data-management-bridge.test.tsx`. Provider implementation and bridge source remain read-only inputs.

**Interfaces:** Consumes public `useDataManagement()` with `busy`, `invalidationEpoch`, `getCurrent()` and app `useQuitStore`; produces `boundary` and `readBoundary()` props. Existing `DataManagementHost`, `DataManagementProvider`, `DataManagementBridge`, `createAppRouter` and palette `navigation.open_home` remain their owners. Rendered scalar state schedules refresh; a live reader blocks stale publication/navigation synchronously.

- Planned checkpoint skipped (see execution deviations): Start with a compilable HomeEntry delegating to HomeRoute without the boundary, then add failing assertions for stale reset rows/navigation and provider lifecycle. Run `pnpm test src/app/home-entry.test.tsx src/app/app-router.test.tsx src/app/search-entry.test.tsx src/app/data-management-bridge.test.tsx --maxWorkers=1 --no-file-parallelism`. Expected red: an old project remains visible or navigable after the live epoch changes, or the router still renders the unadapted entry. An import error is not accepted as the red evidence.
- [x] Connect props and replace only the index element; no key based on epoch/location on the provider tree. Keep `/`, breadcrumb and sidebar contract. Stabilize the live callback or avoid making callback identity a reload dependency. Updating unrelated provider state must not retrigger an unbounded Home query/effect loop.
- [x] Add a real memory-router/provider harness in `home-entry.test.tsx` or `app-router.test.tsx`, using real provider state and mocked IPC. Capture the stable operation API/store identity via `getCurrent` or another existing stable method; do not assert the context snapshot object is stable. Navigate Home → Settings → Home during a deferred operation, prove the same operation continues and no extra prepare/confirm/cancel/subscription is caused by navigation. Use real router composition, not the existing bridge suite's mocked `useNavigate` as evidence of this property.
- [x] Test reset while `/` remains mounted, reset returning from Settings, import while runtime survives, event/result arriving in either order, deferred old project/session responses, failed post-reset refresh and reset uncertainty. Trigger actual existing `acceptCommitted`/operation APIs with fake callbacks; do not manually forge the expected final DOM state. Import must not call terminal clear, reset must preserve the established owner's clear behavior.
- [x] Test pre-effect live epoch change, busy without epoch change, leaving busy with fresh reads, rejected provider bootstrap query, listener setup rejection, late promise after unmount, StrictMode attach/dispose replay and unrelated context updates. Inject failures through mocked IPC rejection/deferred listeners; expect loading/error/degraded UI, no stale rows/navigation, no unhandled rejection or duplicate outstanding listeners. The adapter retains the existing required-context error and never installs a replacement provider. A missing-provider error is not a new standalone test; the production and integration paths use the real required provider.
- [x] Verified the existing `navigation.open_home` command regression together with new HomeRoute and real-router HomeEntry coverage. Search tests remained unchanged; the existing executor still routes to `/` with no project context or additional shortcut. This replaces the planned additional palette test with combined existing-command/new-destination coverage.
- [x] Rerun the exact four-file focused command, `pnpm typecheck`, and `pnpm test src/features/settings/data-management-provider.test.tsx src/features/settings/data-management-state.test.ts src/app/app-shell.test.tsx src/app/use-lifecycle-events.test.ts --maxWorkers=1 --no-file-parallelism`. All existing lifecycle behavior remains valid; these latter files are execution-only regressions, not an expanded modification inventory.

### Task 5: Verify, record acceptance evidence and prepare the feature handoff

**Outcome:** Astra medium reports final code/automated evidence, exact changed files and remaining native work without claiming all of Phase 1 completed.

**Depends On:** Tasks 1–4.

**Files:** This active plan; existing uncommitted `00-Docs/02-Frontend/FE-003-home.md` and `00-Docs/02-Frontend/FE-001-application-shell.md` only for material implementation decisions. No historical plan edits.

**Interfaces:** No new production/test API. Consumes the existing final-gate scripts, isolated Rust targets, and acceptance matrix below.

- [x] Execute every final gate sequentially. Record exact commands, timestamps, discovered test counts, exit codes and final build artifact. Rerun only what changed or failed; a stale build from before the last source edit cannot be the final embedded-frontend evidence.
- [x] Verify negative scope by inspecting `git diff --name-only` against FE-003's writable inventory and `git diff -- src-tauri src/bindings src/lib/ipc package.json pnpm-lock.yaml vite.config.ts` (expected empty for this slice). Inspect Home imports/calls with `rg -n '(@/features/|@/app/|invoke\(|createSession|openProject\(|getProjectGitStatus|setObservedSession)' src/features/home`; existing Welcome Add Project flow is retained, not removed merely by this search. Home dashboard must not add direct invoke, cross-feature imports or mutations. Search findings require interpretation against the pre-existing files, not a false zero-match demand for all Home files.
- [x] Check UTF-8, whitespace, source inventory and assertions for absence of future-phase UI. Preserve user changes; do not stage unrelated files or rewrite historical plans.
- [x] Fill the matrix with actual observations. If native isolation cannot be established, mark those rows pending with the missing environment/measurement reason. Do not run reset against the current profile, simulate a native pass or add new instrumentation commands.
- [x] Handoff to the parent: changed-file inventory, tests/build evidence, deviations, pending acceptance and readiness for the user-requested later `Implement FE003` commit. Keep the two input designs and this plan with the implementation. Parent owns that commit; this plan author performs no commit, delegation or independent review.

## Final Verification

Use repository root for all commands. First set `$env:CARGO_BUILD_JOBS = '1'` in the execution shell; this is a build concurrency setting, not a user-data isolation seam. Do not repurpose system directory variables. Execute each command separately and wait for completion before the next.

| Scope | Command | Expected Result |
|---|---|---|
| Frontend format | `pnpm format:check` | No formatting errors |
| Frontend lint | `pnpm lint` | No lint errors |
| Frontend types | `pnpm typecheck` | No type errors |
| Full frontend | `pnpm test --maxWorkers=1 --no-file-parallelism` | All tests discovered and passing, including every named new/changed test and unchanged regression suite |
| Frontend production | `pnpm build` | Current assets built; record advisory separately |
| Rustfmt | `cargo fmt --manifest-path src-tauri/Cargo.toml --check` | No formatting diff |
| Clippy | `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features --jobs 1 -- -D warnings` | All targets/features pass, warnings denied |
| Public integration regression | `cargo test --manifest-path src-tauri/Cargo.toml --all-features --jobs 1 --test projects_commands --test sessions_runtime --test data_management_contract --test app_builder --test export_bindings -- --test-threads=1` | Explicit existing integration targets pass; generated binding content unchanged |
| Full Rust | `cargo test --manifest-path src-tauri/Cargo.toml --all-targets --all-features --jobs 1 -- --test-threads=1` | Entire discovered suite passes; ignored/native tests remain separately accounted for |
| Windows desktop | `pnpm tauri build` | Exit 0; final `src-tauri/target/release/xwork.exe` embeds current frontend |
| Whitespace | `git diff --check` | No whitespace errors |

The package `test:rust` wrapper lacks all-target/all-feature/thread flags; do not use it as a weaker substitute. Focused tests in Tasks 1–4 are the explicit commands selecting each changed test; the full suite additionally covers the unchanged owner regressions.

### Isolation and failure injection

- Frontend: mock all used `src/lib/ipc/*` wrappers in app harnesses, including listener returns and provider startup queries. Reuse generated DTOs. Deferred promises reproduce in-flight mutation/query races; fake clocks only test UI date scheduling. Restore mocks, fake timers, document visibility changes, listeners, router and existing exported store reset helpers after each case; do not mutate process-global environment for data paths.
- Provider initialization failure: reject `listProjects` with `persistenceFailed`; initial `/` shows error/Retry, never a false Welcome. Reject a session query after projects succeed; projects remain usable. Reject event registration; query still runs, degraded-live warning is visible and Retry does not leak listeners. Resolve an old promise after epoch change/unmount; it cannot publish or navigate.
- Existing Rust tests keep their established injected storage/temp directories, fake participant/OS seams and explicit fixture processes. No new OS test is introduced. Current Windows CLI credential round-trip uses an isolated account/cleanup guard; PTY tests use temporary roots and fixture scripts. Never replace these with the real XWork profile or real project roots. No `--ignored` native tests are silently enabled by this plan.
- Native smoke requires a verified disposable Windows account/profile/credential scope and temporary project fixtures. If no supported way to prove isolation exists, leave pending rather than inventing an environment variable, endpoint or app-data override.

### Phase 1 acceptance matrix and evidence ledger

Planning evidence is limited to repository inspection and historical recorded results. The FE-015 active-at-the-time plan reports 1,952 frontend tests, 478 full Rust tests, Data 36, App 15, Bindings 10, format/lint/types/Clippy/Rustfmt and final Tauri build passed. HEAD now records the feature commit. These aggregate results are a prior baseline, not fresh FE-003 execution evidence and not proof of any native row below.

| Acceptance from §20 / stage 14 | Existing evidence at planning time | Required FE-003/Phase 1 evidence | Current status |
|---|---|---|---|
| Add/manage existing folders without altering project source | Existing Projects implementation/tests; historical aggregate suite passed | Existing `projects_commands` target + frontend regression pass; disposable native add/locate/remove and source-preservation observations | Fresh Projects/frontend gates passed; native pending |
| Read-only Git status | Existing Project Overview/Git implementation; included in historical suite, no new targeted run here | Full Rust/frontend regressions; native overview of disposable Git fixture shows read-only branch/status | Fresh full Rust/frontend gates passed; native pending |
| Multiple sessions across projects; switching preserves processes | Existing Sessions/Terminal implementation and historical aggregate gates | Home all-status/order/destination tests; existing full runtime suites; actual switch Home/session/tray with surviving process | FE-003 and full runtime gates passed; native pending |
| Tab/pane splits up to four and resizing | Existing FE-007/FE-008 automated baseline | Unchanged full frontend/Rust regressions; actual four-pane input/output/resize with WebView2 | Native pending; no new pass claimed |
| Codex, Claude, shell and custom CLI launch at project root | Existing profile/PTY test seams and historical suite | Full Rust regression plus disposable native CLI compatibility, cwd, alternate screen, Unicode/Vietnamese IME | Native pending; fixture shells do not prove all real CLI compatibility |
| Close hides to tray; Quit warns and no runtime restores after relaunch | Existing lifecycle tests/build baseline | Lifecycle regression; native hide/return/Quit/relaunch demonstrating runtime disposal and persistent metadata | Native pending |
| Home truthful recent/session projections and Welcome | FE-003 specification only; current source still placeholder | Tasks 1–4 and final gates, then native layout/keyboard/theme/font/date/focus | FE-003 code and automated coverage passed; native pending |
| Import/reset/provider lifecycle integrated with Home | Stage 13 automated evidence only; Home not implemented | Real-router/provider identity and epoch tests, disposable native import/reset with Home mounted and no stale data | Real-router/provider, epoch and bridge regressions passed; native pending |
| Performance/accessibility carry-forward | Prior stage native/accessibility, search p95 and Data `<500 ms` explicitly pending | Owner-specific measurement setup/dataset/results and manual WebView2 accessibility evidence; no new Home latency promise | Pending; no metric inferred from test counts or build |

Record for each native observation: isolated environment, build/revision, fixture, steps, actual result and any retained logs/screenshots. Do not mark the whole matrix complete merely because the automated gates pass. Partial code delivery may be committed with pending native evidence explicitly stated; stage/Phase 1 completion remains a separate claim.

## Deviations and Decisions

- User authorized design decisions without questions. FE-003 already resolves wireframe differences and has no open questions; no product-contract change was required while writing this plan.
- Use a real-router/provider harness for lifetime evidence; keep the existing mocked-router bridge tests for collaborator behavior. No provider source changes are pre-authorized.
- Existing FE-002 statement about leaving the route table untouched is superseded only by FE-003/FE-001's documented index-element adapter. Paths and navigation semantics stay the same.
- Production mocks are excluded because the matching backend owners are already integrated. No new package or backend interface is needed.
- Astra medium is the intended implementation handoff. This planning turn does not start that agent or execute any implementation task.

## Planning Outcome (before implementation)

At the planning handoff, implementation was pending and every task checkbox was unchecked. Checkboxes above now reflect execution; the ledger below preserves actual sequencing and skipped checkpoints. No frontend/Rust tests, application startup, native smoke, performance measurements or build were run in this PLAN ONLY turn.

Planning self-check covers UTF-8, template sections, dependency order, exact test paths/commands, required flags, isolation seams, writable inventory and whitespace. Input designs remain uncommitted for the later `Implement FE003` feature commit. No blocker for the Astra medium handoff; native isolation/metrics remain explicitly pending and prevent claiming full stage 14/Phase 1 acceptance.

## Execution Evidence — FE-003 Implementation

Implementation authorized by the user after the planning handoff. Baseline is `92328af`; FE-001 modified, FE-003 and this plan untracked were supplied inputs. No staging, commit, subagent, independent review, native startup or stage 15 work is performed by this implementer. The parent will commit `Implement FE003` and stop.

### Milestone 1 — Query lifetime and dashboard

- Initial behavioral red: `pnpm test src/features/home/use-project-presence.test.ts src/features/home/home-route.test.tsx --maxWorkers=1 --no-file-parallelism`, exit 1, 28 tests discovered, 26 passed / 2 failed: missing project snapshot and missing Recent projects heading.
- Projects and Sessions now share a feature-local query lifetime helper inside the existing `use-project-presence.ts` inventory file. It registers before querying, exposes listener failure, retains same-epoch data on refresh failure, retires invalidated flights, removes deleted IDs and guards live maintenance epochs. No new IPC, DTO, owner store, dependency or backend source.
- Three previous hook assertions assumed query-before-subscription or concurrent manual refreshes. They were replaced with subscription-first and event-coalescing assertions, preserving stale success/rejection coverage. The first run after implementation recorded 3 failures / 25 passes before those contract-aligned test changes.
- Projection/query focused run: 47 tests, 46 passed / 1 failed; English `en-GB` date formatting omitted the required comma. The formatter now composes weekday and calendar date explicitly.
- UI/Home/Welcome focused run: 56/56 passed in 3 files (exit 0), covering both sections, all six statuses, full-project joins, unavailable roots, loading/failure, links, keyboard activation, focus reorder/deletion/external ownership and local-midnight rollover.
- Typecheck caught an obsolete direct placeholder import in `welcome-screen.test.tsx`. FE-003 inventory was extended before updating this test: remove only the retired placeholder test and import; retain Welcome tests. No Welcome production source change.
- Workflow deviation: after the initial behavioral red, the remaining projection, Sessions and HomeScreen implementations were built together and tested rather than manufacturing the separate stub/red checkpoints proposed in Tasks 2–3. Their actual failures/results are recorded above; no unexecuted red gate is claimed.

### Milestone 2 — Real router/provider composition

- `pnpm test src/app/home-entry.test.tsx src/app/app-router.test.tsx src/app/search-entry.test.tsx src/app/data-management-bridge.test.tsx --maxWorkers=1 --no-file-parallelism`: exit 0, 100/100 tests in 4 files.
- New HomeEntry harness uses real memory routing, DataManagementHost, DataManagementProvider, DataManagementBridge and public owner stores. Only IPC and terminal/shortcut collaborators are mocked. Stable `getCurrent` identity and persistent child lifetime are asserted across unrelated updates and Home → Settings → Home during deferred export. No extra export/cancel or data subscription occurs.
- Actual `acceptCommitted` and prepare/confirm APIs demonstrate same-route reset, reset returning from Settings, import preserving terminal collaborator identity, failed post-reset reads, late old project/session responses, live click guard, Quit/resume, Welcome without Sessions reads and uncertain reset retaining surviving runtime.
- Existing Search and bridge suites remain separate collaborator regressions; their source tests did not need modification. Their counts are included in the command above; only the real-router HomeEntry harness is used as evidence for router/provider identity.
- Typecheck passes after these changes. Final full gates and additional final regression cases are still pending at this milestone.

### Native / performance acceptance

No verified disposable interactive Windows profile, credential scope or WebView2 measurement setup is available. No app was launched against a real user profile. Every native row of the Phase 1 matrix remains pending: real folder add/locate/remove and source preservation, Git read-only observation, process-preserving navigation, four panes and resize, real Codex/Claude/custom CLI compatibility, tray/Quit/relaunch, Home keyboard/theme/font/contrast/layout, disposable import/reset, search p95 and Data `<500 ms`. Automated tests and build do not satisfy those observations. macOS remains deferred to release preparation.


### Milestone 3 — Final regression adjustments

- Added Welcome listener-degradation notice, retained-Home focus after failed refresh, Home-to-Welcome focus transfer, StrictMode late-listener cleanup and repeated-session-retry locking tests.
- Home-focused run discovered 96 tests: 95 passed / 1 failed. The StrictMode harness needed `reactStrictMode: true` on the RTL root to actually replay effects; production code was unchanged for this fix. Exact Sessions retry then passed 10/10.
- First full frontend gate exposed three sidebar selectors and one shell keyboard-order selector that searched the whole screen. New Home Projects/session links made those selectors ambiguous or incorrectly counted dashboard rows when asserting collapsed sidebar state. The FE-003 inventory now includes `app-sidebar.test.tsx` and `app-shell.test.tsx`; selectors are scoped to the existing sidebar/navigation. Production shell/sidebar behavior is unchanged.
- The initial format/lint/typecheck gates passed; they will be repeated after these final test-scope and source-comment edits. The first full-suite failure remains recorded; final acceptance uses a fresh full run.
- Task 4's artificial stub/red checkpoint was also not performed; the real-router harness was added with the adapter and passed in its first run. No separate red result is claimed for it.

- First full frontend run completed: exit 1, 114 files discovered, 1,996/2,000 tests passed and 4 selector failures (259.45 s). This is not the final passing gate.
- During narrowing an unrelated formatter import reorder, a duplicate `SettingsDataRoute` import caused the first shell/sidebar retry to fail transformation (0 discovered tests). The duplicate was removed; no zero-test result is counted as verification.
- Exact shell/sidebar retry: exit 0, 44/44 tests in 2 files (14.00 s).
- Final-revision format, lint and typecheck: exit 0 each, formatter/linter checked 282 files. A fresh full frontend run is now in progress.

### Milestone 4 — Final automated gates

| Command (repository root) | Actual result |
|---|---|
| `pnpm format:check` | Exit 0; 282 files checked |
| `pnpm lint` | Exit 0; 282 files checked, no lint errors/warnings |
| `pnpm typecheck` | Exit 0 |
| `pnpm test --maxWorkers=1 --no-file-parallelism` | Exit 0; **114 files, 2,000/2,000 tests passed**, 266.14 s |
| `pnpm build` | Exit 0; 2,570 modules; current JS/CSS/WASM output generated |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --check` | Exit 0 |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features --jobs 1 -- -D warnings` | Exit 0 |
| `cargo test --manifest-path src-tauri/Cargo.toml --all-features --jobs 1 --test projects_commands --test sessions_runtime --test data_management_contract --test app_builder --test export_bindings -- --test-threads=1` | Exit 0; Projects 16, Sessions 6, Data 36, App 15, Bindings 10 — **83/83** |
| `cargo test --manifest-path src-tauri/Cargo.toml --all-targets --all-features --jobs 1 -- --test-threads=1` | Exit 0; **478/478 tests passed**; no failed/ignored tests reported |

Cargo build concurrency was one for every Rust command. A subsequent `--list` discovery-only invocation with the same target/feature flags confirmed 478 cases for the aggregate count without rerunning tests. The full suite includes existing isolated ConPTY fixtures and credential test accounts; it is not interactive WebView2/native acceptance. Previously noted terminal stalls and BE-011 timing failures did not recur in this execution.

Production build advisory: JS `index-Bgy3ik_0.js` 1,046.21 kB (gzip 318.35 kB), CSS `index-Dc1Ca09d.css` 58.19 kB, Ghostty WASM 434.45 kB. The existing >500 kB chunk advisory remains; no bundling refactor was authorized. Tauri also reports the existing `.app` bundle-identifier advisory relevant to deferred macOS packaging.



### Final handoff inventory (21 files; status before parent staging)

- `F:/Self Projects/XWork/00-Docs/02-Frontend/FE-001-application-shell.md` (M)
- `F:/Self Projects/XWork/src/app/app-router.test.tsx` (M)
- `F:/Self Projects/XWork/src/app/app-router.tsx` (M)
- `F:/Self Projects/XWork/src/app/app-shell.test.tsx` (M)
- `F:/Self Projects/XWork/src/app/app-sidebar.test.tsx` (M)
- `F:/Self Projects/XWork/src/features/home/home-placeholder.tsx` (D)
- `F:/Self Projects/XWork/src/features/home/home-route.test.tsx` (M)
- `F:/Self Projects/XWork/src/features/home/home-route.tsx` (M)
- `F:/Self Projects/XWork/src/features/home/use-project-presence.test.ts` (M)
- `F:/Self Projects/XWork/src/features/home/use-project-presence.ts` (M)
- `F:/Self Projects/XWork/src/features/home/welcome-screen.test.tsx` (M)
- `F:/Self Projects/XWork/00-Docs/02-Frontend/FE-003-home.md` (??)
- `F:/Self Projects/XWork/00-Docs/98-Plan/20260906-fe-003-home.md` (??)
- `F:/Self Projects/XWork/src/app/home-entry.test.tsx` (??)
- `F:/Self Projects/XWork/src/app/home-entry.tsx` (??)
- `F:/Self Projects/XWork/src/features/home/home-presentation.test.ts` (??)
- `F:/Self Projects/XWork/src/features/home/home-presentation.ts` (??)
- `F:/Self Projects/XWork/src/features/home/home-screen.test.tsx` (??)
- `F:/Self Projects/XWork/src/features/home/home-screen.tsx` (??)
- `F:/Self Projects/XWork/src/features/home/use-home-sessions.test.ts` (??)
- `F:/Self Projects/XWork/src/features/home/use-home-sessions.ts` (??)

## Implementation Outcome

FE-003 Phase 1 Home and the FE-001 stage 14 composition extension are implemented and ready for the parent-owned `Implement FE003` commit. Final `pnpm tauri build` ran with `CARGO_BUILD_JOBS=1`, exited 0 and completed the optimized Rust build in **4m 49s** on 2026-09-06. Artifact: `F:/Self Projects/XWork/src-tauri/target/release/xwork.exe`. Its before-build frontend output matches the final standalone build asset hashes recorded above.

Final code/test revision passed frontend format, lint, types, **2,000 tests / 114 files**, Vite build, Rustfmt, Clippy with warnings denied, **83 selected integration tests**, **478 full Rust tests**, and Tauri Windows build. Source was not changed after the final full frontend gate. Markdown evidence updates followed the build. Final UTF-8 decoding and `git diff --check` passed; no generated binding/backend/IPC/dependency/capability/provider source or historical plan changes were found. HEAD remains `92328af` and nothing is staged.

No interactive desktop E2E or app startup was performed. Native Home layout/accessibility/theme/font checks, disposable import/reset, real CLI/WebView2/tray/Quit observations and previous search/Data metrics remain **pending**, with missing isolation/measurement setup documented in the matrix. Automated ConPTY tests do not close those acceptance rows. The existing large-chunk and bundle-identifier advisories remain. This is a code/automated handoff, not a claim that stage 14 or Phase 1 is fully accepted.

The parent should make the authorized feature commit and then stop, per the user's latest instruction. This implementer has not staged, committed, delegated, conducted an independent review or started stage 15.
